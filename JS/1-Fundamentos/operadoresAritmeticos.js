const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d; //operador infix (binario - entre dois operandos) // operaores postfix (unario - depois do operando) // operador prefix (unario - antes do operando)
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

console.log("Soma=", soma);
console.log("Subtracao=", subtracao);
console.log("Multiplicacao=", multiplicacao);
console.log("Divisao=", divisao);
console.log("Modulo=", modulo);